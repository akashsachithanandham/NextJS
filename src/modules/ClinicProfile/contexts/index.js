'use client';

import React from "react";

export const DoctorCtaContext = React.createContext({});

export const EstablishmentProfileContext = React.createContext({});

export const EstablishmentRelationsContext = React.createContext({});

export const RelationItemContext = React.createContext({});